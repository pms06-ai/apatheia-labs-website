{
  "name": "phronesis-engine-sidecar",
  "version": "0.1.0",
  "description": "FCIP Engine Runner Sidecar for Phronesis",
  "main": "engine-runner.js",
  "scripts": {
    "build": "esbuild engine-runner.ts --bundle --platform=node --target=node18 --outfile=engine-runner.js",
    "start": "node engine-runner.js"
  },
  "dependencies": {
    "sqlite3": "^5.1.7",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}

